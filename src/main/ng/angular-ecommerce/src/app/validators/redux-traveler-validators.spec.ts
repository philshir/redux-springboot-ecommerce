import { ReduxTravelerValidators } from './redux-traveler-validators';

describe('ReduxTravelerValidators', () => {
  it('should create an instance', () => {
    expect(new ReduxTravelerValidators()).toBeTruthy();
  });
});
